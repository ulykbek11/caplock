import { spawn } from "node:child_process";
export interface RunResult { code: number; stdout: string; stderr: string }
export function run(command: string, args: string[], options: { cwd?: string; env?: NodeJS.ProcessEnv; inherit?: boolean; timeoutMs?: number } = {}): Promise<RunResult> { return new Promise((resolve, reject) => { const child = spawn(command, args, { cwd: options.cwd, env: options.env, stdio: options.inherit ? "inherit" : "pipe", detached: process.platform !== "win32" }); let stdout = "", stderr = "", timedOut = false; const timer = options.timeoutMs ? setTimeout(() => { timedOut = true; if (process.platform !== "win32" && child.pid) process.kill(-child.pid, "SIGKILL"); else child.kill("SIGKILL"); }, options.timeoutMs) : undefined; if (!options.inherit) { child.stdout?.on("data", (x: Buffer) => { stdout += x; }); child.stderr?.on("data", (x: Buffer) => { stderr += x; }); } child.on("error", reject); child.on("close", (code) => { if (timer) clearTimeout(timer); resolve({ code: timedOut ? 124 : code ?? 1, stdout, stderr }); }); }); }
export async function commandExists(command: string): Promise<boolean> { try { return (await run(process.platform === "win32" ? "where.exe" : "which", [command])).code === 0; } catch { return false; } }
export const packageManagerCommand = (name: string): string => process.platform === "win32" ? `${name}.cmd` : name;
